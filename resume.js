// Sets the active stylesheet and the page title, based on a CSS title// Thanks to Paul Sowden (http://www.alistapart.com/articles/alternate/)function changeLanguage(css_title, page_title) {	document.title = page_title ;	var i, a, main;	for (i = 0; (a = document.getElementsByTagName("link")[i]) ; ++i) {		if (a.getAttribute("rel").indexOf("style") != -1 && a.getAttribute("title")) {			a.disabled = true;			if (a.getAttribute("title") == css_title) {				a.disabled = false;			}		}	}}$(document).ready(function() {	// Click on a <div> or <li> to choose what's printing or not	$('li,div').click(function () {		// Don't change the class if the element is marked to never print		if (! $(this).hasClass('never-print')) {			$(this).toggleClass('dont-print');		};	});});